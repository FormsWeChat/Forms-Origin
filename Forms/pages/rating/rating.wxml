<template name="rating">
  <view class='com-rating'>
    <block wx:for='{{[1,2,3,4,5]}}' wx:key='*this'>
      <view class='rating-off' bindtap="onClick" data-index="{{index}}">
        <view class='rating-on' 
          style='width:{{rating >= (max/5)*item ? 1 : rating < (max/5)*(item-1) ? 0 : (rating*10)%(max/5*10)/(max/5*10)}}em'
        ><image src='/../../../images/Star_solid.png' mode='widthFix' style='width:0.5em' /></view>
        <view class='rating-off'><image src='/../../../images/Star_hollow.png' mode='widthFix' style='width:0.5em' /></view>
      </view>
    </block>
  </view>
</template>
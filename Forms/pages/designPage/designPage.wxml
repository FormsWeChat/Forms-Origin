<view class="intro">约会小助手</view>
<view class="page-body">
  <view class="page-section">
    <text class="title">"中关村”附近的“湘菜”饭馆有：</text>
    <radio-group class="radio-group" data-id="{{item.id}}">
      <label class="radio">
        <radio value="a#{{item.optiona}}" checked/>
        <text>A{{item.optiona}}</text>
      </label>
      <label class="radio">
        <radio value="b#{{item.optionb}}" />
        <text>B{{item.optionb}}</text>
      </label>
      <label class="radio">
        <radio value="c#{{item.optionc}}" />
        <text>C{{item.optionc}}</text>
      </label>
      <label class="radio">
        <radio value="d#{{item.optiond}}" />
        <text>D{{item.optiond}}</text>
      </label>
    </radio-group>
    <label class="addMore">
    <input class="inputMore" placeholder='添加更多选项'></input>
    <button class="add">添加</button>
    </label>
    <button class="share" bindtouchend='shareForm' open-type='share'>分享</button>
    </view>
  </view>